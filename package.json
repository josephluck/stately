{
  "name": "stately",
  "version": "1.0.0",
  "description": "Functional, immutable state management",
  "main": "./lib/index.js",
  "files": [
    "src/",
    "lib/"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/josephluck/stately"
  },
  "types": "./lib/index.d.ts",
  "scripts": {
    "test": "ts-node node_modules/.bin/tape ./test/*.spec.ts | tap-spec",
    "dist": "rm -rf ./lib && mkdir lib && tsc",
    "deploy": "npm run dist && npm publish"
  },
  "dependencies": {
    "immer": "^5.0.0"
  },
  "devDependencies": {
    "@types/tape": "^4.2.33",
    "tap-spec": "^5.0.0",
    "tape": "^4.11.0",
    "ts-node": "^8.5.0",
    "typescript": "^3.7.2"
  },
  "peerDependencies": {
    "react": "^16.11.0"
  }
}
